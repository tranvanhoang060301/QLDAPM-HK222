import Schema from './schema';
import Query from './query';
declare class Model<T> {
    private readonly collection;
    private readonly schema;
    constructor(collection: string, schema: Schema<T>);
    create(data: T, doc?: string): Promise<T>;
    findById(id: string): Promise<T>;
    find(query: Query): Promise<T[]>;
    findOne(query: Query): Promise<T>;
    updateById(id: string, data: T): Promise<T>;
    update(query: Query, data: T): Promise<T[]>;
    updateOne(query: Query, data: T): Promise<T>;
    deleteById(id: string): Promise<string>;
    delete(query: Query): Promise<string[]>;
    deleteOne(query: Query): Promise<string>;
    private format;
}
export default Model;
